/**
 * Resource - WIP
 */
/*
var Resource = Resource || {};
Resource = (function() {
  
  // Configuration
  var conf = {
    url: 'api/data.json'
  };

  function init() {
    var c     = conf;
    var self  = this;
    this.get();
  }

  function get() {
    var conf = conf;

    $.ajax({
        type: "GET",
        dataType: "json",
        processData: true,
        cache: false,
        jsonp: true,
        url: conf.url
    });
  }

  return {
    init  : init,
    get   : get
  };
})();
$(document).ready(function() {
  console.log('document.ready');
  Resource.init();
  console.log('Resource.init');
});
*/