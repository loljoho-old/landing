$(document).ready(function() {
  console.log('Document Ready');

/**
 * WIP
 */
/*
  var response = $.ajax({
    method    : 'GET',
    //url       : '../api/test.json',
    url: 'test.json',
    dataType  : 'json',
    success   : function(results) {
      console.log('Ajax GET success: ' + results);
    }
  });
*/
});